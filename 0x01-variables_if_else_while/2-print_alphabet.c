#include <stdio.h>

/**
 * main - print alphabet in lower and upercase
 *
 *
 * Return: (0) Success
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
		putchar('\n');

	return (0);
}
