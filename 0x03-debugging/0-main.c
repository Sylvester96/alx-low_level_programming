#include "main.h"
void positive_or_negative(int i);

/**
 * main - tests the function positive_or_negative()
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positve_or_negative(i);

	return (0);
}
